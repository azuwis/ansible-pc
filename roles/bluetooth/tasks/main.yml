---
- name: install bluez
  apt:
    name: bluez
  notify: setcap l2ping

- name: add staffs to bluetooth group
  user:
    name: "{{ item.name }}"
    groups: bluetooth
    append: yes
  with_items: "{{ staff }}"
